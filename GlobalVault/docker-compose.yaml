version: "3.8"

services:
  scraper:
    build: .
    volumes:
      - ./data:/app/data # Mount data directory
      - ./logs:/app/logs # Mount logs directory
    env_file:
      - keys.env
    restart: unless-stopped
